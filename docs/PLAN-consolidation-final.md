# Final Consolidation Plan

## 1. Goal
Complete the integration by updating the skill registry and ensuring all new skills are discoverable.

## 2. Steps
1. Run `generate-registry.js` to index `git-conventions` and `code-reviewer`.
2. Inspect `registry.json` to confirm presence.
3. Clean up the `agent-skills` and `.agent` (root) folders if user agrees (will ask via notify_user).

## 3. Deliverables
- Fully populated `agentskillskit/.agent/skills`.
- Updated Registry.
- Zero legacy folders.
